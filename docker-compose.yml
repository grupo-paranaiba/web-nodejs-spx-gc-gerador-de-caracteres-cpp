services:
  spx-gc:
    build: .
    container_name: spx-gc
    restart: unless-stopped
    ports:
      - "0.0.0.0:3011:3011"
    environment:
      SPX_PORT: "3011"
      SPX_HOST: "0.0.0.0"
    volumes:
      - spx_data:/spx-data

volumes:
  spx_data:
